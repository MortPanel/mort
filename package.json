{
  "name": "root",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "dev": "lerna run --parallel dev"
  },
  "dependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3"
  },
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
